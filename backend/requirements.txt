# ── Web framework ─────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# ── Config & validation ───────────────────────────────────
pydantic>=2.7.0
pydantic-settings>=2.2.0
python-dotenv>=1.0.1

# ── Agentic orchestration ─────────────────────────────────
langgraph>=0.1.9
langchain-core>=0.2.19
langchain-google-genai>=1.0.5
# langchain-openai>=0.1.0  # Uncomment if using OpenAI

# ── Task queue (parallel executor dispatch) ───────────────
celery>=5.4.0
redis>=5.0.4

# ── Database ──────────────────────────────────────────────
psycopg2-binary>=2.9.9
sqlalchemy>=2.0.30
alembic>=1.13.1

# ── Razorpay SDK ──────────────────────────────────────────
razorpay>=1.4.1

# ── PDF generation (template-based, no hallucination) ─────
reportlab>=4.1.0
jinja2>=3.1.4

# ── HTTP client (executor agents → external APIs) ─────────
httpx>=0.27.0

# ── ML metrics (precision/recall on held-out test set) ────
scikit-learn>=1.4.2
numpy>=1.26.4
